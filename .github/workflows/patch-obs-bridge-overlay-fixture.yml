name: Patch OBS Bridge Overlay Fixture

on:
  push:
    branches:
      - feat/ivr-245-stream-controls-themes-performance

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@v4
        with:
          ref: feat/ivr-245-stream-controls-themes-performance
          fetch-depth: 0
      - name: Patch OBS Bridge fixture
        run: node .github/scripts/patch-obs-bridge-overlay-fixture.mjs
      - name: Remove temporary files
        run: rm .github/scripts/patch-obs-bridge-overlay-fixture.mjs .github/workflows/patch-obs-bridge-overlay-fixture.yml
      - name: Commit fixture update
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/obs-bridge/test/obs-bridge.test.ts .github/scripts/patch-obs-bridge-overlay-fixture.mjs .github/workflows/patch-obs-bridge-overlay-fixture.yml
          git commit -m "OBS Bridge fixtureへOverlay既定値を追加"
          git push origin HEAD:feat/ivr-245-stream-controls-themes-performance
